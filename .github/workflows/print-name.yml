name: print-name

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  say-name:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print name
        run: echo "Hello from print-name workflow"
